steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build','-f','Dockerfile.creator','-t','${_REGION}-docker.pkg.dev/$PROJECT_ID/app-repo/mychannel-creator:latest','.']
images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/app-repo/mychannel-creator:latest'
options:
  logging: CLOUD_LOGGING_ONLY




