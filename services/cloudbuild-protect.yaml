steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build','-f','services/protect/infra/Dockerfile','-t','${_REGION}-docker.pkg.dev/$PROJECT_ID/app-repo/mychannel-protect:latest','.']
images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/app-repo/mychannel-protect:latest'
options:
  logging: CLOUD_LOGGING_ONLY
