steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build','-f','services/Dockerfile.transcode','-t','${_REGION}-docker.pkg.dev/$PROJECT_ID/app-repo/mychannel-transcode:latest','services']
images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/app-repo/mychannel-transcode:latest'
options:
  logging: CLOUD_LOGGING_ONLY

