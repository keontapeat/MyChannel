steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build','-f','services/Dockerfile.events','-t','${_REGION}-docker.pkg.dev/$PROJECT_ID/app-repo/mychannel-events:latest','services']
images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/app-repo/mychannel-events:latest'
options:
  logging: CLOUD_LOGGING_ONLY

