{
  "name": "music-payouts",
  "version": "2.0.0",
  "description": "MyChannel Music — Stripe Connect payouts with collaborator revenue splits",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions:music-payouts",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "stripe": "^14.0.0"
  }
}
