Add these to Debug > OTHER_SWIFT_FLAGS in Xcode Build Settings:

-Xfrontend -warn-long-function-bodies=200
-Xfrontend -warn-long-expression-type-checking=200

These will show a WARNING in Xcode when any function body takes > 200ms
to type-check. That tells you EXACTLY what files are still slow.
