'use client';

// 🔥 MyChannel landing home — light mode by default.
// Matches the reference design: hero + sparkline stats + featured player,
// category chips, Featured / AI Recommended, Trending / Top Channels, and a
// sticky sign-in bar. See components/home/LandingHome.tsx.

import LandingHome from '@/components/home/LandingHome';

export default function HomePage() {
  return <LandingHome />;
}
