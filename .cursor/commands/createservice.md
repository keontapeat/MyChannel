# createservice

# Name: Create Service
# Description: Create a new service with singleton pattern

# Prompt:
Create a new service with:
- Singleton pattern (static let shared)
- @MainActor if publishes UI state
- Async methods
- Proper error handling
- Firebase integration (#if canImport)
- Deinit cleanup
This command will be available in chat with /createservice
