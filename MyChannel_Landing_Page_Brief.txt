<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>MyChannel.live - The Future of Video Streaming</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #FF4444;
            --accent: #00FFE1;
            --dark: #0A0A0A;
            --darker: #050505;
            --light: #FFFFFF;
            --gray: #888888;
            --gradient: linear-gradient(135deg, #FF4444 0%, #FF6B6B 100%);
            --glow: 0 0 80px rgba(255, 68, 68, 0.5);
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', sans-serif;
            background: var(--darker);
            color: var(--light);
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* Enhanced Background Animation */
        .bg-animation {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: -1;
            background: linear-gradient(180deg, #0A0A0A 0%, #1A0A0A 100%);
            overflow: hidden;
        }

        .bg-animation::before {
            content: '';
            position: absolute;
            width: 300%;
            height: 300%;
            top: -100%;
            left: -100%;
            background: 
                radial-gradient(circle at 20% 80%, rgba(255, 68, 68, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(0, 255, 225, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 40% 40%, rgba(255, 68, 68, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 60% 70%, rgba(255, 107, 107, 0.06) 0%, transparent 50%);
            animation: float-bg 20s ease-in-out infinite;
        }

        .bg-animation::after {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            background: 
                linear-gradient(45deg, transparent 30%, rgba(255, 68, 68, 0.02) 50%, transparent 70%),
                linear-gradient(-45deg, transparent 30%, rgba(0, 255, 225, 0.01) 50%, transparent 70%);
            animation: gradient-shift 15s ease infinite;
        }

        @keyframes float-bg {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(-20px, -30px) rotate(1deg); }
            66% { transform: translate(20px, 20px) rotate(-1deg); }
        }

        @keyframes gradient-shift {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        /* Navigation */
        nav {
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            padding: 20px 40px;
            background: rgba(10, 10, 10, 0.8);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            transition: all 0.3s ease;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 24px;
            font-weight: 800;
            color: var(--light);
            text-decoration: none;
            transition: transform 0.3s ease;
        }

        .logo:hover {
            transform: scale(1.05);
        }

        .logo-icon {
            width: 45px;
            height: 45px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            box-shadow: 0 4px 20px rgba(255, 68, 68, 0.3);
            overflow: hidden;
            background: var(--gradient);
        }

        .logo-icon::before {
            content: 'MC';
            color: white;
            font-weight: 900;
            font-size: 16px;
        }

        .nav-links {
            display: flex;
            gap: 40px;
            align-items: center;
        }

        .nav-links a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--accent);
            transition: width 0.3s ease;
        }

        .nav-links a:hover {
            color: var(--light);
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .nav-cta {
            background: var(--gradient);
            color: var(--light);
            padding: 12px 28px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 20px rgba(255, 68, 68, 0.3);
            position: relative;
            overflow: hidden;
        }

        .nav-cta::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .nav-cta:hover::before {
            left: 100%;
        }

        .nav-cta:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 30px rgba(255, 68, 68, 0.4);
        }

        /* Hero Section - Clean & Powerful */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 120px 40px 80px;
            position: relative;
            text-align: center;
        }

        .hero-content {
            max-width: 1000px;
            z-index: 2;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(255, 68, 68, 0.1);
            border: 1px solid rgba(255, 68, 68, 0.3);
            color: var(--primary);
            padding: 10px 24px;
            border-radius: 50px;
            font-size: 15px;
            font-weight: 600;
            margin-bottom: 40px;
            animation: pulse-glow 3s ease infinite;
            backdrop-filter: blur(10px);
        }

        @keyframes pulse-glow {
            0%, 100% { 
                box-shadow: 0 0 20px rgba(255, 68, 68, 0.3);
                transform: scale(1);
            }
            50% { 
                box-shadow: 0 0 30px rgba(255, 68, 68, 0.5);
                transform: scale(1.02);
            }
        }

        h1 {
            font-size: clamp(3.5rem, 8vw, 7rem);
            font-weight: 900;
            line-height: 1.1;
            margin-bottom: 30px;
            background: linear-gradient(135deg, #FFFFFF 0%, #FF4444 50%, #00FFE1 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: gradient-text 4s ease infinite;
            background-size: 200% 200%;
            letter-spacing: -0.02em;
        }

        @keyframes gradient-text {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        .hero-subtitle {
            font-size: clamp(1.3rem, 3vw, 1.6rem);
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 50px;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
            line-height: 1.6;
            font-weight: 400;
        }

        .hero-buttons {
            display: flex;
            gap: 24px;
            justify-content: center;
            flex-wrap: wrap;
            margin-bottom: 80px;
        }

        .btn-primary {
            background: var(--gradient);
            color: var(--light);
            padding: 20px 45px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 700;
            font-size: 18px;
            transition: all 0.4s ease;
            box-shadow: 0 10px 40px rgba(255, 68, 68, 0.3);
            position: relative;
            overflow: hidden;
            border: none;
            cursor: pointer;
        }

        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.6s;
        }

        .btn-primary:hover::before {
            left: 100%;
        }

        .btn-primary:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 60px rgba(255, 68, 68, 0.4);
        }

        .btn-secondary {
            background: transparent;
            color: var(--light);
            padding: 20px 45px;
            border-radius: 50px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            text-decoration: none;
            font-weight: 700;
            font-size: 18px;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .btn-secondary::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 0;
            height: 100%;
            background: rgba(255, 255, 255, 0.1);
            transition: width 0.4s ease;
            z-index: -1;
        }

        .btn-secondary:hover::before {
            width: 100%;
        }

        .btn-secondary:hover {
            border-color: rgba(255, 255, 255, 0.6);
            transform: translateY(-4px);
            box-shadow: 0 15px 40px rgba(255, 255, 255, 0.1);
        }

        /* Stats Section */
        .stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 40px;
            max-width: 600px;
            margin: 0 auto;
            padding: 40px 0;
        }

        .stat {
            text-align: center;
            position: relative;
        }

        .stat::before {
            content: '';
            position: absolute;
            top: -20px;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 2px;
            background: var(--gradient);
            opacity: 0.6;
        }

        .stat-number {
            font-size: 3rem;
            font-weight: 900;
            color: var(--primary);
            margin-bottom: 8px;
            animation: count-up 2s ease-out;
        }

        .stat-label {
            color: rgba(255, 255, 255, 0.7);
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 600;
        }

        @keyframes count-up {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* App Preview Section */
        .app-preview {
            padding: 120px 40px;
            background: linear-gradient(180deg, var(--darker) 0%, #0F0505 100%);
            position: relative;
        }

        .preview-container {
            max-width: 1200px;
            margin: 0 auto;
            text-align: center;
        }

        .preview-header {
            margin-bottom: 80px;
        }

        .preview-badge {
            display: inline-block;
            background: rgba(0, 255, 225, 0.1);
            border: 1px solid rgba(0, 255, 225, 0.3);
            color: var(--accent);
            padding: 8px 20px;
            border-radius: 50px;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 25px;
        }

        .preview-title {
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 800;
            margin-bottom: 25px;
            background: linear-gradient(135deg, #FFFFFF 0%, #00FFE1 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .preview-subtitle {
            color: rgba(255, 255, 255, 0.7);
            font-size: 1.2rem;
            max-width: 700px;
            margin: 0 auto 60px;
        }

        .phone-showcase {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 60px;
            flex-wrap: wrap;
            margin: 80px 0;
        }

        .phone-device {
            width: 280px;
            height: 570px;
            background: linear-gradient(135deg, #1A1A1A 0%, #2A2A2A 100%);
            border-radius: 40px;
            border: 3px solid #333;
            position: relative;
            overflow: hidden;
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.6);
            animation: float-phone 4s ease-in-out infinite;
        }

        .phone-device:nth-child(2) {
            animation-delay: -1s;
        }

        .phone-device:nth-child(3) {
            animation-delay: -2s;
        }

        .phone-device:nth-child(4) {
            animation-delay: -1.5s;
        }

        @keyframes float-phone {
            0%, 100% { transform: translateY(0) rotateY(0deg); }
            50% { transform: translateY(-15px) rotateY(2deg); }
        }

        .phone-screen {
            position: absolute;
            top: 25px;
            left: 15px;
            right: 15px;
            bottom: 25px;
            background: #000;
            border-radius: 25px;
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        .screen-header {
            padding: 15px;
            background: rgba(255, 68, 68, 0.1);
            display: flex;
            align-items: center;
            gap: 10px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .screen-content {
            flex: 1;
            padding: 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 20px;
        }

        .feature-icon-large {
            font-size: 60px;
            margin-bottom: 15px;
        }

        .feature-title-phone {
            color: white;
            font-size: 18px;
            font-weight: 700;
            margin-bottom: 10px;
        }

        .feature-desc-phone {
            color: rgba(255, 255, 255, 0.6);
            font-size: 14px;
            text-align: center;
            line-height: 1.4;
        }

        /* Features Grid */
        .features-section {
            padding: 120px 40px;
            background: radial-gradient(circle at center, rgba(255, 68, 68, 0.05) 0%, transparent 70%);
        }

        .features-container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .features-header {
            text-align: center;
            margin-bottom: 80px;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 40px;
        }

        .feature-card {
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, rgba(255, 255, 255, 0.02) 100%);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 40px;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
            backdrop-filter: blur(10px);
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: var(--gradient);
            transform: scaleX(0);
            transition: transform 0.4s ease;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            border-color: rgba(255, 68, 68, 0.3);
            box-shadow: 0 25px 70px rgba(0, 0, 0, 0.6);
        }

        .feature-card:hover::before {
            transform: scaleX(1);
        }

        .feature-icon {
            width: 70px;
            height: 70px;
            background: var(--gradient);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            margin-bottom: 25px;
            box-shadow: 0 10px 30px rgba(255, 68, 68, 0.3);
        }

        .feature-card-title {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 15px;
            color: white;
        }

        .feature-card-description {
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.6;
            font-size: 15px;
        }

        /* Call to Action */
        .cta-section {
            padding: 120px 40px;
            background: linear-gradient(135deg, rgba(255, 68, 68, 0.1) 0%, rgba(0, 255, 225, 0.05) 100%);
            text-align: center;
            position: relative;
        }

        .cta-container {
            max-width: 800px;
            margin: 0 auto;
        }

        .cta-title {
            font-size: clamp(2.5rem, 6vw, 4.5rem);
            font-weight: 900;
            margin-bottom: 30px;
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .cta-subtitle {
            font-size: 1.3rem;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 50px;
            line-height: 1.6;
        }

        .cta-buttons {
            display: flex;
            gap: 25px;
            justify-content: center;
            flex-wrap: wrap;
            margin-bottom: 60px;
        }

        /* Footer */
        footer {
            padding: 80px 40px 40px;
            background: #050505;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            text-align: center;
        }

        .footer-logo {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            font-size: 28px;
            font-weight: 800;
            color: var(--light);
            margin-bottom: 30px;
        }

        .footer-tagline {
            color: rgba(255, 255, 255, 0.6);
            font-size: 1.1rem;
            margin-bottom: 40px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-bottom: 40px;
            flex-wrap: wrap;
        }

        .footer-links a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .footer-links a:hover {
            color: var(--primary);
        }

        .footer-bottom {
            padding-top: 40px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            color: rgba(255, 255, 255, 0.4);
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .hero {
                padding: 100px 20px 60px;
            }

            .hero-buttons {
                flex-direction: column;
                padding: 0 20px;
            }

            .btn-primary, .btn-secondary {
                width: 100%;
                padding: 18px 35px;
                font-size: 16px;
            }

            .stats {
                grid-template-columns: repeat(3, 1fr);
                gap: 20px;
                padding: 30px 10px;
            }

            .stat-number {
                font-size: 2rem;
            }

            .phone-showcase {
                flex-direction: column;
                gap: 40px;
            }

            .phone-device {
                width: 250px;
                height: 500px;
            }

            .features-grid {
                grid-template-columns: 1fr;
                gap: 30px;
            }

            .feature-card {
                padding: 30px 25px;
            }

            .cta-buttons {
                flex-direction: column;
                padding: 0 20px;
            }

            .footer-links {
                flex-direction: column;
                gap: 20px;
            }
        }

        /* Smaller phones */
        @media (max-width: 390px) {
            h1 {
                font-size: 2.2rem;
            }

            .phone-device {
                width: 220px;
                height: 450px;
            }
        }
    </style>
</head>
<body>
    <!-- Background Animation -->
    <div class="bg-animation"></div>

    <!-- Navigation -->
    <nav>
        <div class="nav-container">
            <a href="#" class="logo">
                <div class="logo-icon"></div>
                <span>MyChannel</span>
            </a>
            <div class="nav-links">
                <a href="#features">Features</a>
                <a href="#demo">Demo</a>
                <a href="#creators">Creators</a>
                <a href="#download">Download</a>
                <a href="#" class="nav-cta">Get Early Access</a>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <div class="hero-badge">
                🚀 LAUNCHING THE REVOLUTION
            </div>
            <h1>The Future of Video Has Arrived</h1>
            <p class="hero-subtitle">
                YouTube's reign is over. Welcome to MyChannel — where creators earn 90% 
                revenue share, content is truly free, and innovation drives everything we do.
            </p>
            <div class="hero-buttons">
                <button class="btn-primary">
                    📱 Download on App Store
                </button>
                <button class="btn-secondary">
                    ▶️ Watch Demo
                </button>
            </div>
            <div class="stats">
                <div class="stat">
                    <div class="stat-number">90%</div>
                    <div class="stat-label">Creator Revenue</div>
                </div>
                <div class="stat">
                    <div class="stat-number">100K+</div>
                    <div class="stat-label">Early Adopters</div>
                </div>
                <div class="stat">
                    <div class="stat-number">Zero</div>
                    <div class="stat-label">Hidden Fees</div>
                </div>
            </div>
        </div>
    </section>

    <!-- App Preview Section -->
    <section class="app-preview" id="demo">
        <div class="preview-container">
            <div class="preview-header">
                <div class="preview-badge">📱 EXPERIENCE THE FUTURE</div>
                <h2 class="preview-title">One App. All Entertainment.</h2>
                <p class="preview-subtitle">
                    Long-form videos, Flicks, live streams, music, movies, and monetized tips — 
                    all in one legendary platform that pays creators what they deserve.
                </p>
            </div>
            
            <div class="phone-showcase">
                <div class="phone-device">
                    <div class="phone-screen">
                        <div class="screen-header">
                            <div style="width: 8px; height: 8px; background: #FF4444; border-radius: 50%;"></div>
                            <div style="color: white; font-size: 12px; font-weight: 600;">MyChannel</div>
                        </div>
                        <div class="screen-content">
                            <div class="feature-icon-large">🎬</div>
                            <div class="feature-title-phone">Videos</div>
                            <div class="feature-desc-phone">Professional content with 4K streaming and zero buffering</div>
                        </div>
                    </div>
                </div>
                
                <div class="phone-device">
                    <div class="phone-screen">
                        <div class="screen-header">
                            <div style="width: 8px; height: 8px; background: #00FFE1; border-radius: 50%;"></div>
                            <div style="color: white; font-size: 12px; font-weight: 600;">Flicks</div>
                        </div>
                        <div class="screen-content">
                            <div class="feature-icon-large">⚡</div>
                            <div class="feature-title-phone">Flicks</div>
                            <div class="feature-desc-phone">Short-form content that sparks joy and goes viral instantly</div>
                        </div>
                    </div>
                </div>
                
                <div class="phone-device">
                    <div class="phone-screen">
                        <div class="screen-header">
                            <div style="width: 8px; height: 8px; background: #FF6B6B; border-radius: 50%;"></div>
                            <div style="color: white; font-size: 12px; font-weight: 600;">Live</div>
                        </div>
                        <div class="screen-content">
                            <div class="feature-icon-large">🔴</div>
                            <div class="feature-title-phone">Live Streaming</div>
                            <div class="feature-desc-phone">Real-time streaming with interactive chat and tips</div>
                        </div>
                    </div>
                </div>
                
                <div class="phone-device">
                    <div class="phone-screen">
                        <div class="screen-header">
                            <div style="width: 8px; height: 8px; background: #9D4EDD; border-radius: 50%;"></div>
                            <div style="color: white; font-size: 12px; font-weight: 600;">Music</div>
                        </div>
                        <div class="screen-content">
                            <div class="feature-icon-large">🎵</div>
                            <div class="feature-title-phone">Music</div>
                            <div class="feature-desc-phone">Millions of songs, playlists, and exclusive tracks ad-free</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features-section" id="features">
        <div class="features-container">
            <div class="features-header">
                <div class="preview-badge">🔥 LEGENDARY FEATURES</div>
                <h2 class="preview-title">Everything YouTube Should Have Been</h2>
                <p class="preview-subtitle">
                    Built from the ground up with creators and viewers in mind. No compromises.
                </p>
            </div>
            
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">💰</div>
                    <h3 class="feature-card-title">90% Revenue Share</h3>
                    <p class="feature-card-description">
                        We're not greedy. Creators keep 90% of all revenue. YouTube takes 45%. 
                        The math is simple — you deserve more.
                    </p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">⚡</div>
                    <h3 class="feature-card-title">Flicks Revolution</h3>
                    <p class="feature-card-description">
                        Short-form content reimagined. Our Flicks feature isn't just another copy — 
                        it's what viral content should have been from the start.
                    </p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">📱</div>
                    <h3 class="feature-card-title">Watch Offline</h3>
                    <p class="feature-card-description">
                        Download your favorite videos and watch them anywhere, anytime. No internet? 
                        No problem. Your content is always available.
                    </p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">🤖</div>
                    <h3 class="feature-card-title">AI Discovery</h3>
                    <p class="feature-card-description">
                        Our algorithm actually works. Find content you love, get discovered by 
                        viewers who care. No more fighting the system.
                    </p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">💬</div>
                    <h3 class="feature-card-title">True Community</h3>
                    <p class="feature-card-description">
                        Built-in chat, community features, and direct creator-fan connections. 
                        Your audience is your community, not just viewers.
                    </p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">🎵</div>
                    <h3 class="feature-card-title">MyChannel Music</h3>
                    <p class="feature-card-description">
                        Stream millions of songs ad-free. Music videos, audio tracks, playlists, 
                        and exclusive content. Better than Spotify and YouTube Music combined.
                    </p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">🎬</div>
                    <h3 class="feature-card-title">MyChannel Movies</h3>
                    <p class="feature-card-description">
                        Blockbuster films, indie movies, documentaries, and exclusive originals. 
                        Netflix quality content with creator-friendly revenue sharing.
                    </p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">💸</div>
                    <h3 class="feature-card-title">Tips & Live Monetization</h3>
                    <p class="feature-card-description">
                        Real-time tips during live streams with instant payouts. Fans support 
                        creators directly — no middleman taking massive cuts.
                    </p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">🛡️</div>
                    <h3 class="feature-card-title">Creator Protection</h3>
                    <p class="feature-card-description">
                        Three-strike system with human appeals. Your life's work won't disappear 
                        overnight like on YouTube. We believe in second chances.
                    </p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">⚡</div>
                    <h3 class="feature-card-title">Lightning Fast</h3>
                    <p class="feature-card-description">
                        4K streaming without buffering. Instant uploads. Zero lag live streams. 
                        Built with cutting-edge technology that actually works.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section" id="download">
        <div class="cta-container">
            <h2 class="cta-title">Join The Revolution</h2>
            <p class="cta-subtitle">
                Be part of the platform that's ending YouTube's monopoly. Download now and 
                experience the future of video streaming.
            </p>
            <div class="cta-buttons">
                <button class="btn-primary">📱 Download on App Store</button>
                <button class="btn-secondary">🚀 Get Early Access</button>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <div class="footer-logo">
                <div class="logo-icon"></div>
                <span>MyChannel.live</span>
            </div>
            <p class="footer-tagline">
                The future of video streaming. Where creators thrive, communities grow, 
                and content is truly free. We're not just competing with YouTube — we're replacing it.
            </p>
            <div class="footer-links">
                <a href="#">Features</a>
                <a href="#">Creators</a>
                <a href="#">Privacy</a>
                <a href="#">Terms</a>
                <a href="#">Support</a>
                <a href="#">Press</a>
            </div>
            <div class="footer-bottom">
                © 2025 MyChannel.live — The Revolution Starts Here
            </div>
        </div>
    </footer>

    <script>
        // Enhanced scroll animations
        window.addEventListener('scroll', () => {
            const nav = document.querySelector('nav');
            if (window.scrollY > 50) {
                nav.style.background = 'rgba(10, 10, 10, 0.95)';
                nav.style.boxShadow = '0 5px 20px rgba(0, 0, 0, 0.8)';
            } else {
                nav.style.background = 'rgba(10, 10, 10, 0.8)';
                nav.style.boxShadow = 'none';
            }
        });

        // Smooth scroll for all links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Button interactions
        document.querySelectorAll('.btn-primary, .btn-secondary').forEach(button => {
            button.addEventListener('click', function(e) {
                // Add click animation
                this.style.transform = 'scale(0.98)';
                setTimeout(() => {
                    this.style.transform = '';
                }, 150);

                // Show action based on button text
                if (this.textContent.includes('Download')) {
                    alert('🚀 App Store redirect coming soon! Join our waitlist for early access.');
                } else if (this.textContent.includes('Demo')) {
                    alert('📹 Demo video coming soon! Follow us for updates.');
                } else if (this.textContent.includes('Early Access')) {
                    const email = prompt('Enter your email for exclusive early access:');
                    if (email) {
                        alert(`✅ ${email} added to our VIP early access list!`);
                    }
                }
            });
        });

        // Intersection Observer for animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe elements
        document.querySelectorAll('.feature-card, .stat, .phone-device').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(30px)';
            el.style.transition = 'all 0.8s ease';
            observer.observe(el);
        });

        // Enhanced mouse interactions
        document.addEventListener('mousemove', (e) => {
            const x = (e.clientX / window.innerWidth) * 100;
            const y = (e.clientY / window.innerHeight) * 100;
            
            document.documentElement.style.setProperty('--mouse-x', `${x}%`);
            document.documentElement.style.setProperty('--mouse-y', `${y}%`);
        });

        // Stats counter animation
        const animateCounter = (element, target) => {
            let current = 0;
            const increment = target / 50;
            const timer = setInterval(() => {
                current += increment;
                if (current >= target) {
                    current = target;
                    clearInterval(timer);
                }
                
                if (target === 90) {
                    element.textContent = Math.floor(current) + '%';
                } else if (target === 100000) {
                    element.textContent = Math.floor(current / 1000) + 'K+';
                } else {
                    element.textContent = 'Zero';
                }
            }, 40);
        };

        // Trigger counter animation when stats come into view
        const statsObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const numbers = entry.target.querySelectorAll('.stat-number');
                    numbers.forEach((num, index) => {
                        const targets = [90, 100000, 0];
                        setTimeout(() => {
                            animateCounter(num, targets[index]);
                        }, index * 200);
                    });
                    statsObserver.unobserve(entry.target);
                }
            });
        });

        statsObserver.observe(document.querySelector('.stats'));

        // Console branding
        console.log(`
🚀 MyChannel.live - The Revolution Has Begun
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💪 Built by creators, for creators
🔥 YouTube's monopoly ends here
💎 90% creator revenue share
⚡ Flicks that spark viral moments
🎵 Ad-free music streaming
🎬 Blockbuster movie library
💸 Real-time tips & monetization
📱 Watch offline anywhere
🤖 AI that actually works
💬 True community features
🛡️ Creator protection system

Ready to join the revolution? 
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        `);
    </script>
</body>
</html>